{% extends "::base.html.twig" %}

{% block body -%}
    <h1>Cart edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cart') }}">
            Back to the list
        </a>
    </li>
    <li>
    	<a href="{{ path('cart_show' , { 'id': cart.id }) }}">
    		Show
    	</a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
